update_stories <- function(spreadsheet_name = "(HS) stories", game_folder = "homestreet") {
  event_dates <- hs.balancedata::get_event_dates(game_folder = game_folder)
  season_items <- hs.balancedata::get_season_items()

  return(NA)
}
